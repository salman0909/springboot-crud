apiVersion: apps/v1
kind: Deployment
metadata:
  name: 
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx-app
  template:
    metadata:
      labels:
        app: nginx-app
    spec:
      containers:
        - name: nginx-container
          image: salman1091/nginx-example-01:latest
          ports:
          - containerPort: 80
